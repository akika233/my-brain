pdfplumber>=0.11.0
pypdf>=5.0.0
openpyxl>=3.1.0
requests>=2.31.0

# Optional: recovers supplier names printed only inside logo images. Pure pip, no admin
# rights and no system binary needed, unlike Tesseract. Parsing still works without it.
rapidocr-onnxruntime>=1.3.0
