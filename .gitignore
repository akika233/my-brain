# Local secrets — never commit these
.env
career/.env
telegram-bot/.env
telegram-bot/.venv/
telegram-bot/__pycache__/
career/__pycache__/
career/invoice_extractor/__pycache__/

# Supplier/AP data + generated dashboard (real finance data — keep local)
career/ap_log.csv
career/AP_LG_NF*.CSV
career/supplier_dashboard.xlsx
career/DTC_Retail_2026_Budget_Tracker*.xlsx

# Invoice PDFs + extracted output (may contain finance data)
career/invoice_pdfs/
career/invoices.xlsx
career/invoices.csv
career/invoices.json
career/invoice_extractor_package.zip

# Real supplier invoice scans used as test fixtures — this repo is public, and
# these carry landlord names, IBANs, contract numbers and amounts
career/invoice_extractor/fixtures/ge_rent/
career/ge_rent_invoices.xlsx

# Generated distribution bundle (duplicates the source under career/)
career/invoice_extractor_bundle/
career/invoice_extractor_bundle.zip

# Hevy API cache (local only)
health/hevy-*.json

# Dutch B1 audio transcript cache (local only — may contain book dialogue)
learning/transcripts-cache/

# OS files
.DS_Store
Thumbs.db
